# giving  a variable two digits and taking input
two_digit = input("Type a two digit num? ")
# collecting first from two_digit variable
first_digit = two_digit[0]
# collecting first from second_digit variable
second_digit = two_digit[1]
# adding fist_digit + second_digit
result = int(first_digit) + int(second_digit)
# printing result
print(result)